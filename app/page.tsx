import React from "react";
import AuthBtn from "@/components/Login";
type Props = {};

const page = (props: Props) => {
  return (
    <div>
      hello
      <AuthBtn />
    </div>
  );
};

export default page;
