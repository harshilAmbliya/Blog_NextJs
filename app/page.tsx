import React from "react";
import LoginForm from "@/components/Login";
type Props = {};

const page = (props: Props) => {
  return (
    <div>
      hello
      <LoginForm />
    </div>
  );
};

export default page;
